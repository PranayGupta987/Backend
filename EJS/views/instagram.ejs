<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
</head>
<body>
    <h2>This page belongs to <%= data.name %></h2>
    <button>Follow</button>
    <button>Message</button>
    <p>Followers : <%= data.followers %></p>
    <p>Following : <%= data.following %></p>
    <% for( let index = 0; index < data.posts.length; index++ ) { %>
        <h1>Post</h1>
    <img src="<%= data.posts[index].image %>" alt="Profile Picture" />
    <p>Posts Likes: <%= data.posts[index].likes %></p>
    <p>Posts Comments: <%= data.posts[index].comments %></p>
    
    <% } %>

</body>
</html>